[package]
name         = "openconvert-desktop"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
description  = "OpenConvert desktop app (Tauri 2): spawns the CLI engine as a supervised subprocess"
publish      = false

[[bin]]
name = "openconvert-desktop"
path = "src/main.rs"

# Tauri dependencies are added by the Phase-0 'hello-Tauri' work item, together with
# tauri.conf.json and the externalBin sidecar staging (IMPLEMENTATION_PLAN Phase 0 §7).
[dependencies]
